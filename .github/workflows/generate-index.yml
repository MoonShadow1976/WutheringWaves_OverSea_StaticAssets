name: 生成JSON索引

on:
  push:
    paths:
      - 'data/resource/**'
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-json-index:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: 生成JSON索引
        run: |
          python .github/scripts/generate_json_index.py
      
      - name: 检查并提交变更
        id: check-changes
        run: |
          # 检查是否有JSON文件被创建或修改
          if git status --porcelain | grep -E '\.json$'; then
            echo "changes_detected=true" >> $GITHUB_OUTPUT
          else
            echo "changes_detected=false" >> $GITHUB_OUTPUT
          fi
      
      - name: 提交JSON索引文件
        if: steps.check-changes.outputs.changes_detected == 'true'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # 只添加JSON文件
          git add data/resource/*.json data/resource/*/*.json
          
          git commit -m "📁 自动更新JSON索引 [skip ci]"
          git push